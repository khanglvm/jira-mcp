/**
 * @file path-resolver.ts
 * @description Shared utility for platform-specific path resolution in TUI
 * Re-exports from main source to avoid duplication
 */

import { mapPlatform, resolvePlatformPath } from '../../../src/utils/path-resolver.js';

export { mapPlatform, resolvePlatformPath };
